# Import namespaces and helper functions from __init__.py


def test_import_error():
    # Find way to trigger the import error in testing.
    assert True  # Manual testing shows that it works, though.
